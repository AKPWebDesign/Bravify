var Promise = require('bluebird'); // jshint ignore:line

module.exports = new Promise(function(resolve, reject) {
  resolve(true);

  //check for updated version

    //download updated app.asar
    //restart

  //resolve true if no update required
});
